# Legacy MERN Prototype

The following are **deprecated** and preserved for reference only:

- `client/` — React + Vite digital menu
- `server.js` — Express + MongoDB API
- `app.js`, `index.html`, `styles.css` — vanilla JS version
- `models/`, `seed.js`, `config/db.js`

**Active development** is in:

- `frontend/` — Next.js (CafeOS)
- `backend/` — FastAPI (CafeOS)

Do not add features to legacy code. Migrate assets via `scripts/legacy_menu.json`.
